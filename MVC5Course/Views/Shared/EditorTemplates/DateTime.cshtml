@model DateTime?

@{ 
    var htmlAttributes = new
    {
        @class = "form-control",type = "date"
    };
}

@if (Model.HasValue)
{
    @Html.TextBox("",Model.Value.ToString("yyyy-MM-dd"),htmlAttributes)
}else
{
    @Html.TextBox("","", htmlAttributes)
}